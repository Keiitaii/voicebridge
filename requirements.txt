# Core voicebridge dependencies (Windows + Python 3.12+)
# Install with: pip install -r requirements.txt

# --- Speech recognition & VAD ---
faster-whisper>=1.2.0
silero-vad>=6.0
sounddevice>=0.5
onnxruntime-gpu>=1.20

# --- ML runtime (GPU via NVIDIA pip wheels; the python deps below pull
# the CUDA DLLs that ctranslate2 needs) ---
torch>=2.5
torchaudio>=2.5
nvidia-cublas-cu12>=12.4
nvidia-cudnn-cu12>=9.0
nvidia-cuda-nvrtc-cu12>=12.4

# --- TTS ---
edge-tts>=7.0

# --- Paralinguistic analysis ---
numpy>=1.26
librosa>=0.10
praat-parselmouth>=0.4

# --- Sound-event tagging (YAMNet) ---
tensorflow-cpu>=2.16
tensorflow-hub>=0.16

# --- Emotion (en superb) ---
transformers>=4.40

# --- Emotion (zh emotion2vec via funasr/modelscope; downloads ~1.4 GB
# from modelscope on first use, optional — disable in config if unwanted) ---
funasr>=1.3

# --- Windows integrations ---
pywin32>=305         # COM Scripting (shortcut creation)
pystray>=0.19        # system tray
pillow>=10           # tray icon image
keyboard>=0.13       # backup hotkey lib (Win32 RegisterHotKey is primary)
